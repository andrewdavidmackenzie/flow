name: ADD

# List of inputs must match the list of inputs declared wherever this flow is used
ioSet:
    inputs:
        input:
            name: param1
            data_type: Integer

        input:
            name: param2
            data_type: Integer

    outputs:
        output:
            name: sum
            data_type: Integer