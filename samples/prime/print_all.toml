flow = "all_numbers"

# Generate the range of numbers from 1..20
[[process]]
alias = "numbers"
source = "lib://flowstdlib/math/sequence"
input.start = {Once =  1}
input.step = {Once =  1}
input.end = {Once =  20}

[[process]]
alias = "array"
source = "lib://flowstdlib/data/accumulate"
input.chunk_size = {Always =  20}
input.partial = {Once =  []}

[[connection]]
from = "numbers/sequence"
to = "array/values"

[[connection]]
from = "array/partial"
to = "array/partial"

####### Output
[[process]]
alias = "print"
source = "lib://flowruntime/stdio/stdout"

[[connection]]
from = "array/chunk"
to = "print"

