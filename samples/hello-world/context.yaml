name: hello-world

entities:
      - name: terminal
        datatype: String
        function: stdout

# The context level can contain one flow at most
flow:
     -  name: say hello
        source: flow1.yaml

connections:
      - name: message
        datatype: String
        from: flow/say hello/message
        to: entities/terminal/