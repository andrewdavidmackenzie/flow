name: say hello

# This defines the outputs of this flow
outputs:
    - name: message
      datatype: String

# This describes the connections between things in this flow
connections:
    - name: message
      from: source/message
      to: output/message

# These are source inside this flow
entities:
    - name: message
      source: message.yaml
      datatype: String