## histogram

Compute a histogram and derived statistics from a flat array of grayscale
pixel values (0-255).

Outputs the 256-bin histogram, plus min/max/average brightness and total
pixel count as separate outputs for parallel downstream processing.
