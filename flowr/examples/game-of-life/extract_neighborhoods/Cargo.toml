[package]
name = "extract_neighborhoods"
version = "0.1.0"
authors = ["Andrew Mackenzie <andrew@mackenzie-serres.net>"]
edition = "2021"

[lib]
name = "extract_neighborhoods"
crate-type = ["cdylib"]
path = "extract_neighborhoods.rs"

[profile.release]
debug = false
lto = true
codegen-units = 1
opt-level = 'z'
panic = 'abort'
strip = true

[dependencies]
flowcore = {path = "../../../../flowcore", version = "1"}
flowmacro = {path = "../../../../flowmacro", version = "1"}
serde_json = { version = "1.0", default-features = false }

[workspace]
exclude = ["../../..", ".."]
