## Args (//flowr/args/get.toml)
Get the arguments the flow was executed with

#### Include using
```
[[process]]
alias = "get"
source = "lib://flowr/args/get.toml"
```

#### Inputs

#### Output
* Array of Strings of the command line arguments the flow was invoked with.