function = "count"
description = "Takes a value on it's input and sends the same value on it's output and adds one to the count"
source = "count.rs"
docs = "count.md"
type = "rust"

[[input]]
name = "data" # the data to count the number of instances sent

[[input]]
name = "count" # the previous count
type = "number"

[[output]] # the incremented count of values passed in
type = "number"
