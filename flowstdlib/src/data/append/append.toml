function = "append"
description = "Append two values as strings (accepts strings, numbers, or any JSON value)"
source = "append.rs"
docs = "append.md"
type = "rust"

[[input]]
name = "s1" # first value — converted to string if not already

[[input]]
name = "s2" # second value — converted to string if not already

[[output]]
type = "string" # the concatenated string