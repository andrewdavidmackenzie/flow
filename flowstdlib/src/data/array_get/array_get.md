## Array Get (//flowstdlib/data/array_get)
Get the value at a given index in an array. Returns null if the index
is out of bounds. Also passes through the array for loopback use.

### Include using
```toml
[[process]]
source = "lib://flowstdlib/data/array_get"
```
