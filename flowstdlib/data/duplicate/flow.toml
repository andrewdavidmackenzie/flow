[package]
name = "duplicate"
version = "0.1.0"
authors = ["Andrew Mackenzie <andrew@mackenzie-serres.net>"]
edition = "2018"

[lib]
name = "duplicate"
crate-type = ["cdylib"]
path = "duplicate.rs"

[profile.release]
debug = false
lto = true
opt-level = 's'

[dependencies]
flow_impl = "=0.9.0"
flow_impl_derive = "=0.9.0"
serde_json = "1.0"

[workspace]
exclude = ["../../../"]
